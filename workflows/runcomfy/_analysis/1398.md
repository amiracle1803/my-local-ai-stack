# 1398 — VNCCS Clone | Consistent Character

**Source file:** `_raw/1398__comfyui-vnccs-character-workflow-qwen-image-seedvr2.json`

## RunComfy description
> VNCCS Clone | Consistent Character

## Custom nodes required
| Package | Installed? |
|---|---|
| ComfyUI-VNCCS | ❌ NO |
| rgthree-comfy | ✅ YES |

## Models referenced
None detected in widget values (may be loaded via custom-node defaults).

**Status: ✅ VNCCS NODE INSTALLED & VERIFIED** — `ComfyUI_VNCCS` custom node loads cleanly (43 nodes, 0 import failures). Set up the **light Anima engine** path for 8GB VRAM: `anima-base-v1.0` + `qwen_3_06b_base` + `qwen_image_vae` (already installed) + `anima-turbo-lora-v0.1` (downloaded). The RunComfy 1398 JSON itself targets Qwen-Edit-2511 + SeedVR2 (heavy) — use the shipped `VNCCS_3.0_Step1_CharacterCreator.json` with the Anima engine instead. Optional heavy deps (llama-cpp-python, transparent-background) are lazy-loaded; only needed for Qwen cloner / matting features.

## 8GB VRAM feasibility (RTX 4070 Laptop)
**Grade: 🟡 Easy** — Install 1-2 custom nodes + a few small models

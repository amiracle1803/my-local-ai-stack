The 10-Step Second Brain: Point Claude at an Obsidian Vault and Never Re-Explain Yourself Again
One evening of setup. A vault that files itself at 7 a.m., and a model that opens every session already knowing your work.
A 30-year-old freelance developer in Lisbon kept his best thinking in 5 places at once. A notes app, 30 browser tabs, a Notion board he stopped opening, and 40 archived Claude chats he would never find again. Every project started the same way: 20 minutes rebuilding context from memory, then losing most of it by Friday.
Then he stopped pasting his life into a chat box and started pointing Claude at a folder. One evening of wiring. By the end of the month he opened his Obsidian graph and froze, because the thing knew connections he had forgotten he made. The same build, step by step, written for someone who has never opened Claude Code or Obsidian.
1. Two tools, two jobs

Obsidian is the storage. A free notes app that keeps everything as plain markdown files on your own machine. Notes link to each other with [[double brackets]], and those links form a graph you can see. Claude is the brain on top. It reads the whole vault, files new material where it belongs, links it to what already exists, and answers across all of it. The whole system is text files, so no single model owns it. Point a different one at the folder next year and it still works.
2. Install Claude Code
Download the Claude desktop app from 
[claude.com/download](https://claude.com/download)
 and open the Code tab. That tab is Claude Code, the version that reads and writes files on your computer. It needs a paid plan. Claude Pro runs $20 a month, or $17 billed annually, and the free tier will not run Claude Code. This is the one cost in the build. Everything below is free.
3. Make your vault
Download Obsidian from obsidian.md. Create a new vault, name it brain, pick a folder to store it. That folder is now your second brain. Make one note, type a sentence, then type [[goals]]. The bracketed word turns into a link. That single mechanic, notes pointing at notes, is the thing Claude will do for you at scale.
4. Open a door into the vault
Claude reaches the vault through a plugin. In Obsidian, open Settings, click Community plugins, turn them on, then Browse and install Local REST API. Enable it, open its settings, and copy the API key. Leave Obsidian running. The door only works while the app is open.
5. Wire Claude in with MCP
In the Code tab, paste this with your key swapped in:
claude mcp add-json obsidian-vault '{ "type": "stdio", "command": "uvx",
"args": ["mcp-obsidian"], "env": { "OBSIDIAN_API_KEY": "PASTE-YOUR-KEY",
"OBSIDIAN_HOST": "127.0.0.1", "OBSIDIAN_PORT": "27124" } }'
The plugin shows the key with the word Bearer in front of it. Drop that word. Paste only the string of letters and numbers after it. mcp-obsidian is the most established bridge, with thousands of GitHub stars. Test it: type "list every file in my vault." If Claude reads your notes back, the door is open.
6. Load yourself into the brain
An empty brain is useless, and you should not type your whole profile by hand. Make Claude interview you instead. Paste:
You are setting up my second brain. Interview me ONE question at a time to
build my profile: who I am and what I do, my goals this year, how I want you
to talk to me, my strengths and weaknesses, my current projects. Wait for each
answer. When done, write it all to CLAUDE.md at the vault root, with headers,
so you load it every session.
Answer like you are briefing a new co-founder. The more real your answers, the more the brain knows you. When it finishes, CLAUDE.md holds your context and you stop re-explaining yourself.
7. Steal Karpathy's structure
In April 2026 Andrej Karpathy posted a pattern he called the LLM Wiki. Two folders carry it. raw/ holds immutable sources, the articles, transcripts, PDFs, and screenshots Claude reads but never edits. wiki/ holds the pages Claude writes from them: summaries, concept pages, cross-references. You never write the wiki. Claude compiles it the moment a new source lands, then runs a lint pass that catches broken links and contradictions. In Karpathy's own run, one topic grew to about 100 articles and 400,000 words without him writing a page of it. Tell Claude: "Set up raw/ and wiki/. When I drop a file in raw/, read it, write or update the matching wiki pages, and link them."
8. Teach Claude the Obsidian dialect
Claude writes plain markdown. It does not know [[wikilinks]], callouts, Bases, or Canvas until you show it how. Steph Ango, the CEO of Obsidian, fixed that. His repo kepano/obsidian-skills ships 5 official Agent Skills covering Obsidian's formats: markdown, Bases, Canvas, the CLI, and web extraction. Drop the repo's contents into a .claude folder at your vault root. Now Claude's notes come out hand-built instead of almost-right. The skills follow the open Agent Skills spec, so they run on Codex and other agents too, and you stay unlocked.
9. Add live data, then walk away

Static notes are half a brain. Connect what changes. For Google Calendar, run:
claude mcp add google-workspace uvx workspace-mcp --tools calendar
Grant read access, then say: "Read today's calendar, log what I committed to in each meeting into my tasks, and flag anything without a clear next step." Once your workflows hold, schedule them. Open the Schedule tab, add a daily 7 a.m. task, and tell it to file anything new in raw/, link it, flag notes that have gone stale, and write a 3-line summary of what changed overnight. One rule you do not break: control access with keys, not prompts. "Do not delete this" is a suggestion. A read-only, scoped key is a setting. If an agent can delete a file, one day it will.
10. Skip the wiring with three free repos
If you would rather not build from scratch, the community already packaged the whole thing. AgriciDaniel/claude-obsidian is a self-organizing brain on the Karpathy pattern, with 15 Claude Code skills and presets for PARA, Zettelkasten, and other methods. eugeniughelbur/obsidian-second-brain ships 43 commands like /obsidian-save, /obsidian-daily, and /obsidian-find, and runs across Claude, Codex, and Gemini. coleam00/second-brain-starter interviews you, writes a build plan, then scaffolds the whole thing from markdown and Python. Clone one. Reshape it into your own.
What you have after this
Before, Claude forgets you the second you close the tab. You hold all the context, and you lose most of it. After, a vault holds everything and a model picks up where it left off. Run it for a week and it is a notes app. Run it for a month and it is a reference system. Run it for 6 months and it is a knowledge engine no amount of Googling replaces, because every new note connects to everything already there.
Same subscription. A completely different machine.
I write about Claude, automation, and systems that run while you sleep. Follow if that is your thing. Every team building AI agents hits the same wall.
You start with one prompt and a few tools.
It works.
Then requirements grow. More edge cases. More teams. More risk.
Suddenly your "agent" is a 3,000-word system prompt trying to do five jobs at once.
The fix isn't more prompt engineering.
It's picking the right pattern.
Here are the 15 patterns every production agentic system is built from — and exactly when to use each one.
Before you pick a pattern
Not every task needs an agent.
A task justifies an agent when:
→ A single model call can't produce a reliable result   → The model must choose between tools or data sources at runtime   → The task needs planning, validation, or iterative refinement   → The workflow has real uncertainty that can't be hardcoded
A task usually does NOT need an agent when the input-to-output path is predictable.
Summarization. Classification. Simple extraction. Templated generation.
These are faster, cheaper, and more reliable as direct model calls.
Wrapping them in an agent just adds latency and failure points for zero benefit.

PATTERN 1 — Single Agent
The simplest and most common starting point.
One model. One system prompt. A bounded set of tools.
The model decides which tool to call, observes the result, and keeps going until it has enough to answer.
Real example: A customer support agent that looks up order status, checks shipping, and creates a ticket if it can't resolve the issue — all with 2-3 tools and one clear job.
Use it when: the task is well-defined, the tool set is small, and one agent can hold the full context without getting confused.
It breaks when: you keep adding tools and the system prompt grows past a page. That's the signal you need a different pattern — not a longer prompt.

PATTERN 2 — Multi-Agent Sequential
Specialized agents run in a fixed order. Each one's output feeds the next one's input.
Real example: A contract review pipeline — one agent extracts obligations, the next identifies risks, a third drafts the summary for procurement. The sequence never changes.
Use it when: the workflow has clear, repeatable stages and each stage produces exactly what the next one needs.
It breaks when: the order actually needs to vary based on what's found mid-process. Sequential pipelines assume the path is fixed — if it isn't, you need something more dynamic.

PATTERN 3 — Multi-Agent Parallel
Independent subtasks run simultaneously, then get combined into one view.
Real example: A 2 am production incident. Three agents investigate logs, metrics, and recent deployments at the same time — not one after another — because every minute matters during an outage.
Use it when: the subtasks are genuinely independent and speed matters.
It breaks when: tasks actually depend on each other's results. Forcing dependent work into parallel execution just creates race conditions and incomplete context.

PATTERN 4 — Loop
Repeat a sequence of steps until an exit condition is met.
Real example: A data cleaning agent that profiles messy CSV data, proposes a cleaning plan, checks if it passes quality standards, and retries if it doesn't — up to a capped number of rounds.
Use it when: the task needs multiple attempts, and you can define a clear, checkable stopping condition.
It breaks when: there's no reliable exit condition. Without one, you get runaway costs and a system that might never terminate.

PATTERN 5 — Review and Critique
A judge agent reviews another agent's output, critiques it, and gives specific actionable feedback.
Real example: A generated report gets reviewed by a separate "critic" agent that flags weak claims, missing evidence, or unclear sections before it ever reaches a human.
Use it when: quality matters more than speed, and you want a second opinion baked into the system, not bolted on after.
It breaks when: the critic agent uses the same blind spots as the generator. A reviewer trained on similar assumptions won't catch the same mistakes.

PATTERN 6 — Iterative Refinement
A feedback loop with a quality score threshold. The generator keeps refining until it crosses the bar.
Real example: A marketing copy generator that scores its own draft against brand guidelines, and keeps rewriting until it hits a minimum quality score — not just one pass-fail check, but graded improvement.
Use it when: output quality is genuinely variable and "good enough" has a measurable threshold.
It breaks when: the scoring function is vague or gameable. If the model can inflate its own score without real improvement, the loop just burns tokens.

PATTERN 7 — Coordinator
A central routing agent directs requests to specialized agents based on what's actually being asked.
Real example: Support tickets get routed to billing, technical, account, shipping, or fraud specialists — each with narrow context instead of one agent trying to know everything.
Use it when: you have genuinely different request types that need different context, tools, or decision logic.
It breaks when: the routing itself becomes ambiguous. If requests don't cleanly fall into one category, the coordinator becomes a new bottleneck and source of misrouting.

PATTERN 8 — Hierarchical Task Decomposition
A root agent breaks a complex goal into smaller subgoals, delegates them to specialist workers, then synthesizes everything into one answer.
Real example: "Which 3 countries should we expand into next year?" gets broken into competitive analysis, regulatory research, logistics feasibility, and market sizing — each handled by a different specialist, then combined.
Use it when: the problem is too broad for one reasoning pass but breaks cleanly into independent areas of expertise.
It breaks when: the subgoals aren't actually independent. If workstreams need to inform each other in real time, decomposing them upfront loses that interaction.

PATTERN 9 — Swarm
Multiple specialist agents contribute to a shared discussion, challenge each other's assumptions, and a facilitator synthesizes a final recommendation.
Real example: Should the company launch a subscription tier? Research, engineering, finance, and support agents each argue their perspective across multiple rounds before a facilitator weighs the trade-offs.
Use it when: there's no single "correct" answer — you need a well-reasoned decision shaped by genuinely competing viewpoints.
It breaks when: you need a fast, deterministic answer. Swarms are deliberately slow and exploratory — wrong tool if you need speed.

PATTERN 10 — ReAct (Reason and Act)
The agent alternates between reasoning and action: decide what to investigate, call a tool, observe the result, decide if there's enough evidence yet.
Real example: "The queue processor seems stuck" — the agent searches docs, checks service health, correlates findings, and only then suggests a fix. The investigation path isn't predefined; it depends on what it finds along the way.
Use it when: the path to the answer genuinely can't be planned upfront — it depends on what each step reveals.
It breaks when: investigations run long without converging. Always cap the number of reasoning-action cycles, or you risk infinite exploration.

PATTERN 11 — Human-in-the-Loop
The agent investigates and recommends, but a human makes the final call on anything risky or ambiguous.
Real example: Refund approvals — low-risk, clear-cut cases get automated. High amounts, fraud signals, or policy exceptions pause for human review before anything is finalized.
Use it when: the decision carries real financial, legal, or reputational risk and full automation isn't acceptable yet.
It breaks when: you treat this as just a UI feature instead of an architectural one. You need durable state, reviewer assignment, timeout handling, and escalation paths — not just a "pause" button.

PATTERN 12 — Plan-and-Execute
A planner agent creates a full structured plan upfront — reviewable and modifiable — before any action is taken. An executor then runs through the steps.
Real example: "Resize the worker fleet from 10 to 20 instances, verify the queue drains, update the runbook." The full plan is visible before execution starts, unlike ReAct where the path emerges step by step.
Use it when: you want the plan to be reviewable or approvable before any action happens — important for operations with real consequences.
It breaks when: the environment changes faster than the plan can execute. A stale plan executed blindly is worse than no plan at all.

PATTERN 13 — Reflexion
The agent evaluates its own failures, reflects on what went wrong, and carries that memory into the next attempt.
Real example: A code generation agent writes a script, it fails at runtime, the agent analyzes the actual error, records what to fix, and retries — getting smarter with each attempt instead of repeating the same mistake.
Use it when: failures are informative and self-correction genuinely improves the next attempt.
It breaks when: the failure modes are random or unrelated to each other. Reflexion only helps when there's a real pattern to learn from.

PATTERN 14 — Custom Logic
A hybrid: deterministic code handles the rules that must never be wrong, while the model handles judgment, drafting, and exception handling.
Real example: A refund workflow where purchase verification and fraud checks run as hard deterministic rules — never delegated to the model — while drafting the customer response and routing recommendations stay agentic.
Use it when: the workflow has real branching logic with legal or financial consequences, and you need to be precise about what's deterministic versus what's flexible.
It breaks when: teams blur the line and let the model make decisions that should be hardcoded rules. Eligibility, permissions, and money movement should never be the model's call alone.

PATTERN 15 — Event-Driven Agent
The agent doesn't wait to be asked. It subscribes to an event stream and acts the moment a condition is triggered.
Real example: A fraud detection agent that reacts the instant a suspicious transaction event fires — not when a support ticket eventually surfaces it, by which point the damage is done.
Use it when: timing matters more than anything else, and waiting for a human request means missing the window to act.
It breaks when: the triggering conditions are poorly defined. A noisy event stream with vague triggers turns into a system constantly crying wolf — or worse, missing the real signal.

Pattern selection — match the uncertainty, not the hype
The right pattern matches the shape of the uncertainty in your work:
→ Uncertain which tool to use → Single Agent or ReAct  → Uncertain where to route → Coordinator  → Uncertain about quality → Review & Critique or Iterative Refinement  → Uncertain execution path → Plan-and-Execute or ReAct  → Uncertain how to self-correct → Reflexion or Loop  → Uncertain about business risk → Human-in-the-Loop or Custom Logic  → Uncertain problem structure → Hierarchical Decomposition or Swarm  → Can't wait for a request → Event-Driven Agent
A swarm is not more advanced than a single agent if the task only needs one reliable tool call.
Plan-and-Execute is not an upgrade from ReAct if your plan goes stale by step three.
The most reliable production systems are not the most autonomous ones.
They put autonomy exactly where it creates value — and constrain it everywhere else.

10 rules for production agentic systems
Start with the smallest pattern that works. A single agent with clean tool contracts beats a multi-agent system with weak ones.
Write tool descriptions like contracts. The model only knows what the tool does from the description — not from your intent.
Cap iterations, tool calls, and spend per request. An agent without budget limits is a liability waiting to show up in a bill.
Log the full action trace. Tool calls, arguments, outputs, final decision. Without this, incident investigation is guesswork.
Keep irreversible actions behind deterministic checks or human approval. Never let a model be the only gate before a money movement or production change.
Evaluate with real failure cases, not just happy paths. Happy-path correctness is a prototype. Edge-case correctness is a product.
Separate prompts by responsibility before the system prompt becomes unreadable. "But don't do X when Y" creeping into your prompt means the agent is doing two jobs.
Treat multi-agent systems as distributed systems. Partial failure, timeouts, retries, and observability are not optional.
Model review is not a substitute for deterministic validation. Use judges to improve quality. Use tests and permission checks to enforce correctness.
Prefer the simpler pattern — not because simple is always better, but because the complexity budget you save can be spent on better tools, better prompts, better evaluation.


That's all 15.
Most teams don't fail because they picked the wrong pattern.
They fail because they never asked which uncertainty they were actually solving for.
Pick the pattern. Match the shape of the problem. Don't add autonomy where it doesn't earn its place.


If this was useful:
For more breakdowns like this  → Bookmark this — you'll come back to it every time you start a new agent project→ Repost to share it with every engineer building agents on your team  → Follow 
[@ai_rohitt](https://x.com/@ai_rohitt)
 
I write about AI, building products, and systems that work without you. Loop Engineering Clearly Explained 
Half your feed is suddenly saying the same thing. Stop prompting your agents, start engineering loops.
Boris Cherny, the person who built Claude Code, said it plainly: "I don't prompt Claude anymore. I have loops that are running. My job is to write loops."
The person who builds one of the most popular coding agents on earth doesn't prompt it. So what is he doing instead?
That's the whole idea behind loop engineering. Now let's break down why it's harder than it looks.
First, the loop itself
An agent isn't a magic box. At its core, it's a plain loop:
python
while True:
    response = model(context)
    if response.has_tool_calls():
        results = run_tools(response.tool_calls)
        context += results
    else:
        break

The model reads the context. It asks to call a tool. You run the tool and feed the result back. The model reads again, and this repeats until it stops asking for tools.
Model → tools → context → repeat.
Here's the part that surprises people. This loop is already solved. Every serious agent framework lands on roughly these six lines. Nobody is competing on the while statement.
So if the loop is trivial, what is everyone actually engineering?
The work moved outside the model
The center of gravity in AI keeps drifting away from the model itself.
Prompt engineering. The words you send.
Context engineering. Everything the model sees, not just your instructions.
Harness engineering. The code around the model that runs tools, tracks state, and handles errors.
Loop engineering. The autonomous cycle that drives the whole thing toward a goal.
Each layer wraps the one before it. You didn't stop caring about prompts. You just realized the prompt is one small piece of a much bigger system.
LangChain puts it cleanly. Agent = Model + Harness. If you're not the model, you're the harness.
And here's the finding that should reorder your priorities. The harness now matters more than the model. Teams have kept the model fixed, changed only the code around it, and jumped from the middle of a benchmark into the top five. Same brain, different loop.
Loop engineering is the discipline of building everything that brain runs inside. Let me show you the parts that actually break.
Hard part 1: knowing when to stop
This is the problem nobody warns you about.
When an agent stops asking for tools, it has ended its turn. That is not the same as finishing the job.
Picture a coding agent. It writes some code, glances around, sees that progress was made, and announces it's done. The tests still fail. It declared victory anyway.
A terminal message ends the turn, not the task. Confusing those two is the most common way loops go wrong.
Good loops stop for the right reasons, so you layer several brakes:
Max iterations. A hard cap so a stuck agent can't run forever.
Budget and time limits. A ceiling on tokens, money, and seconds.
No-progress detection. If it repeats the same call with the same arguments, it's spinning.
A real completion check. An automated condition proving the job is done.
That last one carries the weight. "Done" should mean the tests pass, not the agent feeling good about its work.
Hard part 2: keeping the context clean
Long loops rot from the inside.
The more turns an agent takes, the more junk piles into its context, like old tool outputs, dead ends, and stale reasoning. Model performance drops as that pile grows. The field calls it context rot.
A loop makes it spiral. A rotted context produces a worse decision, which adds more noise, which rots the context further. People call this the doom loop, and you've felt it. The agent gets dumber the longer it runs.
You fight it by treating context as a budget, not a bucket:
Compaction. Summarize the conversation when it gets long, then continue from the summary.
Offloading. Push huge outputs to a file and keep only the slice you need.
Sub-agents. Hand a messy subtask to a separate agent and let only its clean result return.
The instinct is to keep everything, just in case. The skill is knowing what to throw away.
Hard part 3: tools the agent can actually use
A loop is only as good as the tools inside it.
Pile on a hundred tools and the agent loses track of which one to reach for. A tight set of focused, non-overlapping tools wins. Anthropic's rule of thumb is sharp. If a human engineer can't say for certain which tool fits, the agent has no chance.
Two things matter more than people expect:
Make writes safe to repeat. Loops retry, and if a retried "create customer" call makes a second customer, you'll wake up to duplicate records and double billing. Anything that changes state has to be safe to call twice.
Write error messages for the agent, not the human. A good error tells the agent what to do next. Before a tool ships, ask whether an LLM reading its error would know the next move.
In a loop, an error isn't a dead end. It's the next instruction.
Hard part 4: something that can say no
Autonomous loops have a quiet failure mode. An agent left alone tends to agree with itself.
The sharpest comment in the whole debate nailed it. Designing the loop is half the job, and the other half is putting something in the loop that can say no, like a test, a type check, or a real error.
A loop with no critic is just an agent nodding along to its own work.
The fix is to separate the maker from the checker. One model does the work. A different check, often a separate model or a hard test, grades it. The worker doesn't grade its own homework.
The actual shift
Now Cherny's quote makes sense.
Prompting is you steering the agent move by move. Loop engineering is you building the system that steers it, then stepping back.
Your job changes from giving instructions to designing three things:
The goal, written as success criteria the agent can check itself against.
The loop, with sane brakes so it stops well.
The verifier, so "done" is proven, not claimed.
Andrej Karpathy captures the mindset. Don't tell the model what to do, give it success criteria and watch it go. He runs research loops overnight that tweak a script, test it, keep what works, and discard what doesn't, with himself nowhere in the loop. He arranges it once and hits go.
That's the whole move. You stop being the hands and become the person who designs the machine.
Where to start
You don't need an overnight autonomous agent on day one. Build up to it:
Start with the basic loop, and add a max-iteration cap, a timeout, and a cost ceiling right away.
Define "done" as an automated check before you begin, not a vibe afterward.
Protect the context. Compact long runs, offload big outputs, isolate messy subtasks.
Audit your tools. Keep them few and focused, make writes safe to repeat, and rewrite errors so an agent can act on them.
Put a critic in the loop. Only go fully hands-off once you trust the thing that says no.
The takeaway
Loop engineering isn't a framework or a tool you install. It's a shift in where you aim your effort.
The model is becoming a commodity. The loop around it is where the real engineering lives now.
The best builders stopped asking "what should I tell the agent to do?" They started asking "what system would do this without me?"
Answer that one well, and you'll stop prompting too.
Here's a summary of

Key takeaways in loop engineering.
Thanks for reading!  take this info and give a comprehensive analysis of this, pull out and keep keypoints so i can feed it into claude

Here’s a concise but **comprehensive synthesis** you can feed into Claude as a durable knowledge note. The core idea across both pieces is the same: stop treating the model as a one-shot chatbot and instead treat it as part of a looped system with memory, verification, and clear stop conditions. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments
Both texts argue that the real shift in AI work is not “better prompting,” but **better system design** around the model. In the second-brain piece, Claude becomes the active layer on top of an Obsidian vault; in the loop-engineering piece, Claude becomes the active layer inside a controlled execution loop. In both cases, the model is only useful when it can read context, take actions, and have its output checked against something durable and external. [computer](https://www.computer.org/csdl/proceedings-article/pact/2025/829500a186/2cuFCrrt4ze)

## Key point map

- **Context should live outside the model.** Obsidian stores the durable knowledge base as plain markdown, while Claude reads and writes against it; the loop article says the same thing in engineering terms by pushing context, tools, and verification into the harness instead of the prompt. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)
- **The right abstraction is a loop, not a prompt.** The loop article explicitly frames agents as repeated model → tool → observation cycles with bounded iterations and explicit stop handling, while the second-brain article uses the same logic to keep Claude filing, linking, and summarizing automatically. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/images/92537422/e8aee502-8465-4b89-849a-0bcfee974240/image.jpg?AWSAccessKeyId=ASIA2F3EMEYEZMZQVEO4&Signature=3KaTgVSuJmszUuNmQWj%2FZXtcOgg%3D&x-amz-security-token=IQoJb3JpZ2luX2VjEKP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLWVhc3QtMSJHMEUCIGWuXnpJw9%2F9x1BbyV0ZIwD9OdiZZK2dSA6cPX2lAqDNAiEA2pte%2FxmPfEy5JL8zoktZjcYk97c9JoVpE%2Fxy%2FX%2Fc6iEq8wQIbBABGgw2OTk3NTMzMDk3MDUiDIHlqqe4U9lOHyCmkirQBAECULxzZ6Aoo1fAA%2BA5xeluO8Wk4X02Ctl7AotANPC92v48K8S%2FH1acXeFTc0wtF9rQCBa%2BdRTa0YvtFXpwdM1kQA9E6x4l0MFbcF%2FLemfXPyN4sCCYZJrwOANkV0MemV%2FXANSOPl1LdEAJDCNWqNYr3cDiydGeQCLpokpDJOmVPIcvK1Q1wjQe9UoQltXrRBQy7kenvTQU9Z7DoPrPmQTdPyAU%2B6EfgNpTe6KEYNlzYEtHA03IBXNmeH7y3RLzNoFBDEV8YXENtmzmdPxI5clgHXwiQPhP%2FvUtlwjMbfIS16TjSPSVedW32KFXuE%2Bo62eQl1nfVNKFRyTNBDGtjtik%2FmvntpHlFcoww1KhyMVAeqqXxdkX7nJ8JyWaLdDKyxnXgwGhYY7C29LyoA9v7Bm6z%2Fzl60ThsZki9uTtKVaqkAKtF4khykXPf8YOSVGLz9OHzc9ARr%2BArKXDPSSq6tzkIdlmhlRwDzr4fC5t1ZA3cGsUF%2FbyGdWvNG3284Nvw0NQ0aMoZtwrIjzn3Dk6CdJ3iqdA5qo3ns4q1KeEbvOO9kWpDJBB36fQPDsYZSDxlupbm6OSTaOu%2F4E3Ryg3v%2B%2Fk0vggpXyChMzVkXQAMrAAY19CPAzEWDGMQFAtz%2BI8lviHxQf33iOczmX4QdmXpPFKpASzY2pxv4eIHpn3Bp%2FUDOt53AU%2FvQ2JlkR5MU6sYqQtVMuSzNF7huAtZn2UQw4oBYc%2FfLp2dbHu1kBy6adW6kJGGMcmkrUeOimafeLdA02d3YROJsUvfSAE50WIR28wwov70QY6mAGI7JfGLzP3RRDiHcbpA4lWNrG42Phumjp0FOx7eE7c8A54Qse0Rrs%2BglTjxQBB27BFLHSKjwo8n3TdyBzPv%2FYefnF9cigtvUmxQt48S0vsURXq5vxQW4UYkOhL8qrQUNxL3Fjrrgl7pMcJscBagnl0d%2Bp4vRPgTGN%2B7hz6A%2FM5bB5ERQucESsyXisESNOD3PjHl7MTES3kGA%3D%3D&Expires=1782502293)
- **Verification matters.** The second-brain setup uses structured vault files and skills to keep outputs consistent, while the loop article stresses critics, tests, hard checks, and deterministic gates so the model does not declare victory on vibes. [computer](https://www.computer.org/csdl/proceedings-article/pact/2025/829500a186/2cuFCrrt4ze)
- **Scope must be constrained.** Both pieces recommend narrow tools, scoped access, and explicit permissions rather than letting the model improvise with unlimited authority. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)
- **Memory is the product.** The second-brain architecture is about accumulating personal or project memory into a searchable system; the loop article is about preserving enough clean state to keep reasoning correct over many iterations. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/images/92537422/e8aee502-8465-4b89-849a-0bcfee974240/image.jpg?AWSAccessKeyId=ASIA2F3EMEYEZMZQVEO4&Signature=3KaTgVSuJmszUuNmQWj%2FZXtcOgg%3D&x-amz-security-token=IQoJb3JpZ2luX2VjEKP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLWVhc3QtMSJHMEUCIGWuXnpJw9%2F9x1BbyV0ZIwD9OdiZZK2dSA6cPX2lAqDNAiEA2pte%2FxmPfEy5JL8zoktZjcYk97c9JoVpE%2Fxy%2FX%2Fc6iEq8wQIbBABGgw2OTk3NTMzMDk3MDUiDIHlqqe4U9lOHyCmkirQBAECULxzZ6Aoo1fAA%2BA5xeluO8Wk4X02Ctl7AotANPC92v48K8S%2FH1acXeFTc0wtF9rQCBa%2BdRTa0YvtFXpwdM1kQA9E6x4l0MFbcF%2FLemfXPyN4sCCYZJrwOANkV0MemV%2FXANSOPl1LdEAJDCNWqNYr3cDiydGeQCLpokpDJOmVPIcvK1Q1wjQe9UoQltXrRBQy7kenvTQU9Z7DoPrPmQTdPyAU%2B6EfgNpTe6KEYNlzYEtHA03IBXNmeH7y3RLzNoFBDEV8YXENtmzmdPxI5clgHXwiQPhP%2FvUtlwjMbfIS16TjSPSVedW32KFXuE%2Bo62eQl1nfVNKFRyTNBDGtjtik%2FmvntpHlFcoww1KhyMVAeqqXxdkX7nJ8JyWaLdDKyxnXgwGhYY7C29LyoA9v7Bm6z%2Fzl60ThsZki9uTtKVaqkAKtF4khykXPf8YOSVGLz9OHzc9ARr%2BArKXDPSSq6tzkIdlmhlRwDzr4fC5t1ZA3cGsUF%2FbyGdWvNG3284Nvw0NQ0aMoZtwrIjzn3Dk6CdJ3iqdA5qo3ns4q1KeEbvOO9kWpDJBB36fQPDsYZSDxlupbm6OSTaOu%2F4E3Ryg3v%2B%2Fk0vggpXyChMzVkXQAMrAAY19CPAzEWDGMQFAtz%2BI8lviHxQf33iOczmX4QdmXpPFKpASzY2pxv4eIHpn3Bp%2FUDOt53AU%2FvQ2JlkR5MU6sYqQtVMuSzNF7huAtZn2UQw4oBYc%2FfLp2dbHu1kBy6adW6kJGGMcmkrUeOimafeLdA02d3YROJsUvfSAE50WIR28wwov70QY6mAGI7JfGLzP3RRDiHcbpA4lWNrG42Phumjp0FOx7eE7c8A54Qse0Rrs%2BglTjxQBB27BFLHSKjwo8n3TdyBzPv%2FYefnF9cigtvUmxQt48S0vsURXq5vxQW4UYkOhL8qrQUNxL3Fjrrgl7pMcJscBagnl0d%2Bp4vRPgTGN%2B7hz6A%2FM5bB5ERQucESsyXisESNOD3PjHl7MTES3kGA%3D%3D&Expires=1782502293)

## What the second-brain article is really saying

The article is not just about Obsidian setup; it is proposing a **personal knowledge operating system**. The vault is the canonical source of truth, and Claude is a worker that can organize, connect, and summarize that source without requiring you to restate your life every session. The use of `CLAUDE.md`, `raw/`, and `wiki/` creates a separation between stable profile context, raw inputs, and generated knowledge artifacts. The important architectural idea is that your “brain” becomes portable because it is plain text and folder structure, not vendor-locked chat history. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)

## What the loop article is really saying

The loop article is arguing that the hardest part of production AI systems is not the model, but the **harness**: the code that decides when to call tools, when to stop, how to validate, and how to recover from failure. It emphasizes that some tasks should remain simple single-call workflows, while others justify an agentic loop only when uncertainty, tool choice, or multi-step refinement is genuinely required. It also highlights the common failure modes: runaway loops, stale context, too many tools, poor stop conditions, and agents that cannot say no to themselves. The article’s main practical point is that reliable autonomy comes from constraints, not from bigger prompts. [computer](https://www.computer.org/csdl/proceedings-article/pact/2025/829500a186/2cuFCrrt4ze)

## Shared design principles

| Principle | Second-brain article | Loop-engineering article |
|---|---|---|
| Durable memory outside model | Obsidian vault as plain markdown knowledge store  [codelibrary](https://www.codelibrary.ai/mcp/obsidian) | Context offloaded, compacted, or externalized to prevent rot  [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/images/92537422/e8aee502-8465-4b89-849a-0bcfee974240/image.jpg?AWSAccessKeyId=ASIA2F3EMEYEZMZQVEO4&Signature=3KaTgVSuJmszUuNmQWj%2FZXtcOgg%3D&x-amz-security-token=IQoJb3JpZ2luX2VjEKP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLWVhc3QtMSJHMEUCIGWuXnpJw9%2F9x1BbyV0ZIwD9OdiZZK2dSA6cPX2lAqDNAiEA2pte%2FxmPfEy5JL8zoktZjcYk97c9JoVpE%2Fxy%2FX%2Fc6iEq8wQIbBABGgw2OTk3NTMzMDk3MDUiDIHlqqe4U9lOHyCmkirQBAECULxzZ6Aoo1fAA%2BA5xeluO8Wk4X02Ctl7AotANPC92v48K8S%2FH1acXeFTc0wtF9rQCBa%2BdRTa0YvtFXpwdM1kQA9E6x4l0MFbcF%2FLemfXPyN4sCCYZJrwOANkV0MemV%2FXANSOPl1LdEAJDCNWqNYr3cDiydGeQCLpokpDJOmVPIcvK1Q1wjQe9UoQltXrRBQy7kenvTQU9Z7DoPrPmQTdPyAU%2B6EfgNpTe6KEYNlzYEtHA03IBXNmeH7y3RLzNoFBDEV8YXENtmzmdPxI5cl
53AU%2FvQ2JlkR5MU6sYqQtVMuSzNF7huAtZn2UQw4oBYc%2FfLp2dbHu1kBy6adW6kJGGMcmkrUeOimafeLdA02d3YROJsUvfSAE50WIR28wwov70QY6mAGI7JfGLzP3RRDiHcbpA4lWNrG42Phumjp0FOx7eE7c8A54Qse0Rrs%2BglTjxQBB27BFLHSKjwo8n3TdyBzPv%2FYefnF9cigtvUmxQt48S0vsURXq5vxQW4UYkOhL8qrQUNxL3Fjrrgl7pMcJscBagnl0d%2Bp4vRPgTGN%2B7hz6A%2FM5bB5ERQucESsyXisESNOD3PjHl7MTES3kGA%3D%3D&Expires=1782502293)
- Obsidian works as the storage layer; Claude works as the reasoning and filing layer. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)
- `CLAUDE.md` should hold persistent identity, goals, communication style, current projects, strengths, and weaknesses. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)
- `raw/` should store immutable source material, and `wiki/` should store Claude-generated synthesized knowledge. [codelibrary](https://www.codelibrary.ai/mcp/obsidian)
- The safest agent systems use small tool sets, scoped permissions, explicit stop conditions, and deterministic validation. [ppl-ai-file-upload.s3.amazonaws](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/images/92537422/e8aee502-8465-4b89-849a-0bcfee974240/image.jpg?AWSAccessKeyId=ASIA2F3EMEYEZMZQVEO4&Signature=3KaTgVSuJmszUuNmQWj%2FZXtcOgg%3D&x-amz-security-token=IQoJb3JpZ2luX2VjEKP%2F%2F%2F%2F%2F%2F%2F%2F%2F%2FwEaCXVzLWVhc3QtMSJHMEUCIGWuXnpJw9%2F9x1BbyV0ZIwD9OdiZZK2dSA6cPX2lAqDNAiEA2pte%2FxmPfEy5JL8zoktZjcYk97c9JoVpE%2Fxy%2FX%2Fc6iEq8wQIbBABGgw2OTk3NTMzMDk3MDUiDIHlqqe4U9lOHyCmkirQBAECULxzZ6Aoo1fAA%2BA5xeluO8Wk4X02Ctl7AotANPC92v48K8S%2FH1acXeFTc0wtF9rQCBa%2BdRTa0YvtFXpwdM1kQA9E6x4l0MFbcF%2FLemfXPyN4sCCYZJrwOANkV0MemV%2FXANSOPl1LdEAJDCNWqNYr3cDiydGeQCLpokpDJOmVPIcvK1Q1wjQe9UoQltXrRBQy7kenvTQU9Z7DoPrPmQTdPyAU%2B6EfgNpTe6KEYNlzYEtHA03IBXNmeH7y3RLzNoFBDEV8YXENtmzmdPxI5clgHXwiQPhP%2FvUtlwjMbfIS16TjSPSVedW32KFXuE%2Bo62eQl1nfVNKFRyTNBDGtjtik%2FmvntpHlFcoww1KhyMVAeqqXxdkX7nJ8JyWaLdDKyxnXgwGhYY7C29LyoA9v7Bm6z%2Fzl60ThsZki9uTtKVaqkAKtF4khykXPf8YOSVGLz9OHzc9ARr%2BArKXDPSSq6tzkIdlmhlRwDzr4fC5t1ZA3cGsUF%2FbyGdWvNG3284Nvw0NQ0aMoZtwrIjzn3Dk6CdJ3iqdA5qo3ns4q1KeEbvOO9kWpDJBB36fQPDsYZSDxlupbm6OSTaOu%2F4E3Ryg3v%2B%2Fk0vggpXyChMzV
IR28wwov70QY6mAGI7JfGLzP3RRDiHcbpA4lWNrG42Phumjp0FOx7eE7c8A54Qse0Rrs%2BglTjxQBB27BFLHSKjwo8n3TdyBzPv%2FYefnF9cigtvUmxQt48S0vsURXq5vxQW4UYkOhL8qrQUNxL3Fjrrgl7pMcJscBagnl0d%2Bp4vRPgTGN%2B7hz6A%2FM5bB5ERQucESsyXisESNOD3PjHl7MTES3kGA%3D%3D&Expires=1782502293)

## Claude-ready note

You can paste this into Claude as a compact enduring brief:

> I am building a second-brain system where Obsidian is the durable markdown memory layer and Claude is the reasoning/filing layer. Use `CLAUDE.md` for persistent identity, goals, preferences, communication style, and active projects. Use `raw/` for immutable source material and `wiki/` for generated synthesis pages. Prefer explicit structure, linked notes, and daily summaries over freeform chat memory. Keep tool access scoped and safe. Treat Claude as a worker inside a controlled harness, not as the source of truth. Use iterative loops only when the task genuinely needs planning, refinement, tool choice, or verification. Always prefer external memory, deterministic checks, and clear stop conditions over “just trust the model.”

I can also turn this into a **cleaner Claude.md template** or a **vault bootstrap plan** tailored to your Obsidian setup.

How to Build Self-Improving AI Agents with Loop Engineering
Most AI agents do not fail because the model is weak.
They fail because the system around the model is weak.
A prompt is vague.
A tool call breaks.
The agent retries the wrong thing.
It forgets what it learned two steps ago.
It repeats the same bad action.
It declares success too early.
And nobody built the feedback loop that would have caught the mistake.
That is the real problem.
If you want to build AI agents that actually improve over time, you need more than better prompts, more tools, or more autonomy.
You need loop engineering.
Loop engineering is the discipline of designing how an agent:
observes what happened
evaluates whether it worked
updates its next move
stores useful feedback
retries or escalates intelligently
gets better instead of just getting busier
That shift matters.
Because the next generation of useful AI agents will not be defined by how impressive they look in one demo run.
They will be defined by whether they can:
recover from mistakes
learn from repeated failures
improve quality without human babysitting every turn
stay bounded, measurable, and reliable in production
That is what this article is about.
Want more practical AI systems like this?
ByteBuilders shares short, useful breakdowns on Claude, AI workflows, prompts, automation, and real-world engineering systems.
Subscribe here:

[https://bytebuilders.beehiiv.com/subscribe](https://bytebuilders.beehiiv.com/subscribe)
If you understand loop engineering, you stop building fragile agents that only work when everything goes right.
You start building systems that improve because they were designed to learn.
First, what “self-improving” actually means
A lot of people hear “self-improving agent” and imagine a system that magically retrains itself or evolves into something autonomous and mysterious.
That is not the useful definition.
A self-improving agent is usually much simpler.
It is an agent that can use signals from previous attempts to make better decisions on the next attempt.
That can happen through:
retries with better context
evaluation-based revision
memory of previous failures
tool-use corrections
output scoring
human feedback loops
rule updates
stored successful patterns
In other words:
self-improvement is usually not model evolution. It is loop evolution.
The base model may stay the same.
What improves is the system’s behavior around the model.
That is a much more practical and engineering-friendly frame.
Why most agents do not improve on their own
A surprising number of agents are not actually designed to improve.
They are designed to continue.
That is not the same thing.
Many agent systems today are basically:
read task
think
call tool
continue until stopped
The problem is that this structure often lacks:
explicit success criteria
failure classification
state-aware retries
memory of what already failed
comparison between attempts
output evaluation
escalation rules
So the agent does not improve.
It just loops.
That is the difference between raw iteration and intelligent iteration.
Loop engineering is what creates that difference.
Loop engineering is the real architecture layer
The easiest way to understand loop engineering is this:
Prompting tells the model what to do.
Loop engineering tells the system how to behave after it tries.
That includes questions like:
What should the agent do after a failed tool call?
When should it retry versus switch strategies?
How many attempts are allowed?
What should be stored in memory?
What should be evaluated automatically?
When should a human take over?
What counts as improvement?
This is why loop engineering belongs in the architecture conversation, not just the prompt conversation.
The core loop behind self-improving agents

Most useful self-improving agents follow a repeated structure that looks something like this


plaintext
Goal
  ↓
Plan
  ↓
Act
  ↓
Observe result
  ↓
Evaluate quality
  ↓
Store signal
  ↓
Retry / revise / escalate / finalize
That is the foundation.
The system improves when each pass through the loop changes the next decision in a useful way.
Not every agent needs a complex version.
But every reliable agent needs some version of this.
The difference between a naive agent and a loop-engineered agent
Dimension
Naive Agent
Loop-Engineered Agent
Task handling
attempts task once or blindly continues
works through bounded cycles
Error response
retries randomly or fails hard
retries based on classified failure
Memory
little or no useful state
stores actionable context
Evaluation
assumes completion means success
checks outputs against criteria
Improvement
accidental
designed
Human role
only emergency fallback
deliberate escalation point
Reliability
inconsistent
progressively stronger over time
This is the shift.
The loop-engineered agent is not necessarily “smarter” at the model level.
It is just operating inside a better system.
The five building blocks of self-improving agents

If you want an agent to improve, you need a loop with structure.
These five building blocks matter most.
1. Clear success criteria
If the system does not know what “good” looks like, it cannot improve toward it.
Success criteria can be:
exact output format
correctness threshold
grounded answer requirement
tool completion signal
passing test result
human approval
task-specific score
Without this, the agent has nothing stable to optimize around.
2. Evaluation layer
The evaluation layer is what tells the agent whether its attempt worked.
This can be:
rule-based checks
schema validation
unit tests
LLM-as-judge scoring
retrieval grounding checks
business logic validation
human review
This is the difference between “it produced an answer” and “it produced a useful answer.”
3. Feedback memory
Self-improvement requires memory, but not just raw history.
The agent needs usable memory.
Good feedback memory includes things like:
last failure reason
previous tool error
best-known successful path
known bad strategy to avoid
user preference or correction
compressed summary of prior attempts
Not all context should persist.
Only the context that helps the next decision.
4. Strategy revision
After evaluation, the agent must decide what changes.
That may include:
trying a new tool
narrowing the task
asking a clarifying question
retrieving more information
switching from action to explanation
escalating to a human
This is where the system actually becomes adaptive.
5. Boundaries and stop conditions
A self-improving agent still needs control.
Otherwise you do not get improvement.
You get chaos.
Set limits for:
retries
cost
latency
destructive actions
human approval thresholds
task abandonment rules
A bounded loop is more valuable than an unconstrained one.
What loop engineering looks like in practice

Here is a simple pattern that works well for many agents.
Loop 1: attempt
The agent tries the task with current context.
Loop 2: evaluate
A check runs.
Examples:
did the schema validate?
did the code pass tests?
did the answer cite the required source?
did the tool return the right fields?
Loop 3: diagnose
If it failed, classify why.
Examples:
missing context
bad tool arguments
retrieval failure
hallucinated assumption
incomplete output
policy conflict
Loop 4: adapt
Change the next attempt.
Examples:
retrieve more context
rewrite the task more narrowly
try a different tool
add validation rules
ask user for missing detail
Loop 5: store useful signal
Save only what helps future runs.
Then repeat.
That is loop engineering in operational terms.
A practical diagram for loop-engineered agents

plaintext
User Goal
   ↓
Task Interpreter
   ↓
Planner
   ↓
Tool / Action Layer
   ↓
Result
   ↓
Evaluator
   ├── pass → finalize
   ├── fail: missing context → retrieve more
   ├── fail: bad tool use → revise action
   ├── fail: low confidence → escalate
   └── fail: recoverable → retry with memory
This is where agent quality becomes systematic instead of accidental.
Where self-improvement signals should come from
This is one of the most important design questions.
If you feed the wrong signals into the loop, the agent may optimize in the wrong direction.
Strong sources of improvement signals include:
Deterministic signals
tests pass or fail
schema validates or fails
API status is success or error
output contains required fields or not
policy rule is satisfied or violated
Workflow signals
retry count
tool latency
step where failure occurred
confidence threshold crossed
escalation frequency
Human signals
thumbs up / thumbs down
corrected output
accepted versus rejected draft
manual override patterns
reviewer notes
Comparative signals
version A versus version B output quality
retrieval variant comparison
tool path comparison
score improvement over repeated runs
The best self-improving agents usually combine at least two of these categories.
The best loops improve the workflow, not just the response
This is where the design gets more mature.
A weak implementation uses loops only to regenerate text.
A stronger implementation uses loops to improve the whole workflow.
That means the agent can improve by changing:
what it retrieves
what tools it uses
the order of steps
what it remembers
what it ignores
whether it asks for clarification earlier
whether it stops sooner
This is much more powerful than “rewrite the answer again.”
The system becomes self-correcting at the process level.
Prompt patterns that support self-improvement

Prompting still matters.
It just needs to support the loop.
Here are useful prompt patterns.
Prompt 1: evaluation-aware action prompt
plaintext
You are executing a task inside a bounded workflow.
Your goal is not just to produce an answer, but to produce an answer that will pass evaluation.
If information is missing, ask for it or retrieve it.
If the result is uncertain, do not finalize confidently.
If a previous attempt failed, avoid repeating the same strategy unless the context changed.
Prompt 2: failure-diagnosis prompt
plaintext
The previous attempt failed.
Classify the likely failure reason into one of these categories:
- missing context
- tool misuse
- bad assumption
- incomplete output
- formatting or schema failure
- policy conflict
Then propose the smallest next change that improves the odds of success.
Prompt 3: revision prompt
plaintext
Revise the next attempt using the evaluator feedback below.
Do not repeat the same reasoning path if it already failed.
Prefer a narrower, more grounded solution.
If needed, retrieve more context before answering.

Prompt 4: memory summary prompt
plaintext
Summarize the last attempt into a short memory block for the next run.
Include only:
- what failed
- what worked
- what should be avoided
- what should be tried next
Keep it under 120 words.
These are not magic prompts.
They are prompts designed to reinforce loop behavior.
A simple Python-style loop architecture
Below is a simplified example of what loop engineering can look like in code
python
MAX_ATTEMPTS = 4

memory = []

for attempt in range(MAX_ATTEMPTS):
    context = build_context(task=task, memory=memory)
    plan = agent.plan(context)
    result = agent.act(plan)

    evaluation = evaluator.check(task=task, result=result)

    if evaluation.passed:
        return {
            "status": "success",
            "result": result,
            "attempt": attempt + 1,
        }

    failure_summary = agent.summarize_failure(
        result=result,
        evaluation=evaluation,
    )

    memory.append({
        "attempt": attempt + 1,
        "failure": failure_summary,
        "suggested_fix": evaluation.next_step,
    })

    if evaluation.requires_human:
        return {
            "status": "escalated",
            "reason": evaluation.reason,
            "attempt": attempt + 1,
            "memory": memory,
        }

return {
    "status": "failed",
    "reason": "max_attempts_reached",
    "memory": memory,
}
This is intentionally simple.
The important point is not the code itself.
It is the structure:
attempt
evaluate
store signal
adapt
retry or escalate
That is the pattern to internalize.
Where loop engineering matters most
This approach is especially valuable in workflows where success is not guaranteed on the first pass.
Coding agents
Perfect for loops because the system can:
run tests
inspect failures
revise code
compare outputs
stop only when verification passes
Support agents
Useful because the system can:
retrieve policy context
draft a response
check grounding
ask for approval if risk is high
Research agents
Helpful because the system can:
gather multiple sources
identify gaps
search again
synthesize only when evidence is sufficient
Document extraction agents
Useful because the system can:
parse files
validate schema
retry ambiguous fields
escalate low-confidence cases
Operational workflow agents
Valuable because the system can:
call APIs
check outcomes
recover from partial failure
route exceptions to humans
In all of these cases, loop quality matters more than one-shot generation quality.
Common mistakes in loop engineering
Mistake 1: treating retries as improvement
More attempts do not automatically mean better learning.
If the system repeats the same behavior, it is not improving.
It is just looping.
Mistake 2: storing too much memory
Not all past context helps.
Too much memory can pollute the next attempt.
Store compressed, actionable lessons—not raw transcript piles.
Mistake 3: skipping evaluation
Without a check, the agent cannot know if it got better.
This is the most common structural failure.
Mistake 4: no stop condition
An unconstrained agent is not advanced.
It is expensive.
Mistake 5: ignoring human feedback
If humans keep correcting the same thing and the loop never uses that signal, the system is not really improving.
Mistake 6: using one giant loop for every task
Different tasks need different loops.
A support agent and a coding agent should not share the same retry logic or evaluation rules.
The practical build sequence I recommend
If you are building from scratch, use this order.
Step 1: define success clearly
What does a good result look like?
Step 2: define failure categories
Why does this agent usually fail?
Step 3: build the evaluator first
How will the system know it succeeded or failed?
Step 4: design memory rules
What should persist between attempts?
Step 5: define retry strategy
What changes between attempt one and attempt two?
Step 6: add escalation logic
When should a human step in?
Step 7: log everything important
You cannot improve what you do not observe.
Step 8: optimize only after the loop works
Do not overcomplicate the system before the core cycle is stable.
That sequence keeps teams focused on behavior instead of hype.
A simple checklist before you call an agent “self-improving”
Use this as a reality check.
Does the agent know what success looks like?
Does it evaluate each attempt?
Does it store useful lessons from failure?
Does it change strategy based on feedback?
Does it avoid repeating the same failed path blindly?
Does it know when to stop?
Does it know when to escalate to a human?
Can you measure whether it is actually getting better?
If most of these are missing, the system is probably not self-improving.
It is just iterative.
Final thoughts
The future of AI agents is not just better prompts, more tools, or longer chains.
It is better loops.
That is what loop engineering makes possible.
It turns an agent from a one-shot generator into a system that can:
observe
evaluate
adapt
remember
retry intelligently
improve under real conditions
That is what self-improvement actually looks like in production AI.
Not mystery.
Not magic.
Just better system design.
If you want stronger agents, do not just ask how to make them act.
Ask how to make them learn from what happened next.
That is where the real leverage is.

You can think of this article as a design spec for building agents that *actually get better* run over run, by upgrading your loop (goal → action → evaluation → adaptation) instead of just your prompt. It treats “self‑improvement” as a property of the **system around the model** rather than the model itself.

***

## Core thesis

The article’s core claim is that most agents fail because their **surrounding system is weak**, not because the base model is bad.  
Typical failures—bad retries, forgotten context, premature “success,” repeated mistakes—are all symptoms of missing or poorly designed feedback loops rather than model IQ problems.  

It reframes “self‑improving agent” from sci‑fi model retraining to something much more practical: an agent that uses signals from previous attempts (tests, failures, human feedback, metrics) to make **better decisions next time**, while the underlying model often stays fixed.  

***

## What “self‑improving” actually means

The article defines self‑improvement as **loop evolution**, not model evolution.  
A self‑improving agent is one where each pass through the loop changes the next decision in a useful way: richer context, better tool choices, refined prompts, updated rules, or smarter escalation.  

Concretely, improvement can come from: retries with extra context, evaluation‑driven revision, remembering prior failures, fixing tool use, scoring outputs, incorporating human feedback, and encoding successful strategies as reusable rules.  
This matches research patterns like Self‑Refine and evaluator–optimizer loops, which improve outputs via generate→critique→refine cycles without retraining the model itself. [agentwiki](https://agentwiki.org/self_refine)

***

## Why naive agents do not improve

Most real‑world “agents” today are just **continuation machines**: they read the task, think, call tools, and keep going until someone stops them.  
They usually lack explicit success criteria, structured failure types, state‑aware retries, memory of failed attempts, comparison between attempts, output evaluation, and escalation rules.  

As a result, they “loop” but do not *learn*: retries are random, context is noisy or forgotten, and completion is treated as success by default.  
The article calls this the difference between **raw iteration** (just doing more steps) and **intelligent iteration** (steps that are guided by feedback).  

***

## Loop engineering vs. prompt engineering

Prompt engineering focuses on “what to tell the model right now.”  
Loop engineering focuses on “what the *system* should do after the model responds”: how it reacts to success or failure, when it retries, what it records, how it adapts, and when it escalates.  

Key design questions move from the prompt into the architecture: How does the agent respond to a failed tool call? When does it retry vs. switch strategy? How many attempts are allowed? What gets stored as memory? When does a human intervene? What is considered improvement?  
This is why the article positions loop engineering as an **architecture discipline**—similar to how evaluator–refine patterns and reflection loops are treated in production agentic systems. [docs.aws.amazon](https://docs.aws.amazon.com/prescriptive-guidance/latest/agentic-ai-patterns/workflow-for-evaluators-and-reflect-refine-loops.html)

***

## The canonical self‑improvement loop

The article’s core loop is:

> Goal → Plan → Act → Observe result → Evaluate quality → Store signal → Retry / revise / escalate / finalize  

The system becomes self‑improving when each pass through this loop **feeds back** into the next pass: the evaluation changes the strategy, the memory changes the context, and the retry logic changes the action.  
Not every agent needs a complex version of this, but any agent you call “self‑improving” needs some version of it that is explicit, bounded, and observable.  

***

## Naive vs. loop‑engineered agents

The comparison table in the article highlights several key shifts:

- **Task handling**  
  - Naive: attempts the task once or blindly continues.  
  - Loop‑engineered: works in bounded cycles with explicit stop criteria.
- **Error response**  
  - Naive: retries randomly or fails hard.  
  - Loop‑engineered: retries based on *classified* failure types.
- **Memory**  
  - Naive: little or no useful state, or unstructured transcript blobs.  
  - Loop‑engineered: stores compact, actionable context (what failed, what worked, what to avoid).
- **Evaluation**  
  - Naive: assumes “I produced an answer” equals success.  
  - Loop‑engineered: checks outputs against clear criteria.
- **Improvement**  
  - Naive: accidental (if it gets better, it’s luck).  
  - Loop‑engineered: designed (feedback is part of the architecture).
- **Human role**  
  - Naive: last‑resort emergency.  
  - Loop‑engineered: deliberate escalation point with defined triggers.  

The punchline: the loop‑engineered agent is not necessarily smarter per token, but **operates inside a better control system**.  

***

## Five building blocks of self‑improving agents

The article’s main design framework is five components that every self‑improving loop needs.

### 1. Clear success criteria

If the system does not know what “good” looks like, it cannot move toward it.  
Success criteria might be a strict output format, correctness threshold, groundedness requirement, tool completion signal, passing tests, human approval, or a task‑specific score.  

These criteria become the **target** the loop optimizes around—similar to how test suites and structured evaluators define “done” in evaluator–optimizer architectures. [hopx](https://hopx.ai/blog/ai-agents/evaluator-optimizer-loop/)

### 2. Evaluation layer

The evaluation layer says whether an attempt “worked.”  
This can be rule‑based checks, schema validation, unit/integration tests, LLM‑as‑judge scoring, retrieval‑grounding checks, business logic guards, or human review.  

It is the difference between “the agent produced a string” and “the agent produced a *useful* result.”  
Modern agentic patterns commonly treat this as an explicit evaluator component, sometimes implemented as another LLM or as a deterministic checker. [docs.aws.amazon](https://docs.aws.amazon.com/prescriptive-guidance/latest/agentic-ai-patterns/evaluator-reflect-refine-loop-patterns.html)

### 3. Feedback memory

You want **usable** memory, not a dumping ground.  
Good feedback memory stores compressed information about: failure reasons, tool errors, successful paths, known‑bad strategies, user preferences, and short summaries of prior attempts.  

The principle is: persist only context that affects future decisions; everything else can be truncated or summarized to avoid context rot.  

### 4. Strategy revision

After evaluating a result, the system must decide *what changes* for the next attempt.  
Revision can be: switching tools, narrowing or rewriting the task, asking clarifying questions, retrieving more data, changing from action to explanation, or escalating to a human.  

This is where the agent becomes **adaptive** instead of just persistent—feedback has to actually change the policy.  

### 5. Boundaries and stop conditions

Self‑improving does not mean unbounded.  
You need limits on retries, token cost, latency, destructive actions, and escalation thresholds.  

A bounded loop is more valuable than an unbounded one: it stays debuggable, predictable, and affordable, and it protects you from agents that endlessly thrash or chase edge cases.  

***

## Practical loop pattern (operational view)

The article proposes a simple operational pattern broken into five loop stages:

1. **Attempt** – Agent tries the task with current context.  
2. **Evaluate** – Run checks: schema validation, tests, grounding, tool response sanity.  
3. **Diagnose** – Classify failure causes (missing context, tool misuse, bad assumption, incomplete output, policy conflict).  
4. **Adapt** – Change the next attempt (more retrieval, different tool, narrower task, extra validation, ask the user).  
5. **Store signal** – Write down only what will help future attempts.  

Then repeat until criteria are met, a limit is hit, or escalation is triggered.  
This general loop matches many research and production designs (generate → evaluate → refine) while staying implementation‑agnostic. [agentwiki](https://agentwiki.org/self_refine)

***

## Architecture diagram: where pieces sit

The article also gives a higher‑level architecture:

> User Goal → Task Interpreter → Planner → Tool/Action Layer → Result → Evaluator → {finalize | retrieve more | revise action | escalate | retry with memory}  

Important points:

- The **task interpreter** and **planner** turn a vague goal into structured steps.  
- The **tool/action layer** is where API calls, code execution, or environment changes happen.  
- The **evaluator** controls what happens next, not the generator alone; it routes to finalize, retry with more context, revise tooling, or escalate to humans.  

That structure makes improvement **systematic** instead of accidental: every run goes through the same evaluation and adaptation gates.  

***

## Where improvement signals should come from

The agent’s evolution is shaped by the signals you feed into the loop.  
The article breaks them into four useful categories:

- **Deterministic signals** – tests pass/fail, schema validation, HTTP status codes, presence of required fields, policy rules satisfied/violated.  
- **Workflow signals** – retry count, latency, which step failed, confidence thresholds, escalation frequency.  
- **Human signals** – thumbs up/down, corrected outputs, approvals vs. rejections, reviewer notes, overrides.  
- **Comparative signals** – A/B output quality comparisons, different retrieval or tool paths, score improvements across attempts.  

Strong systems usually combine at least two sources—for example, “tests pass AND evaluator score above threshold” or “passes schema AND human approval for high‑risk cases.” [globaltechcouncil](https://www.globaltechcouncil.org/ai/self-improving-ai-agents/)

***

## Improving the workflow, not just the output

A weak implementation uses the loop only to regenerate text (“try again with a slightly different prompt”).  
A stronger implementation lets the loop change *the workflow itself*:

- what to retrieve and when  
- which tools to call and in what order  
- when to ask clarifying questions  
- what to remember vs. discard  
- when to stop early rather than over‑refine  

That makes the system **process‑level self‑correcting**, not just output‑level.  

***

## Prompt patterns that support the loop

The article gives four prompt archetypes that align with loop engineering:

1. **Evaluation‑aware action prompt** – Reminds the agent that its goal is to pass evaluation, not just answer; encourages asking for info or retrieving more context instead of bluffing.  
2. **Failure‑diagnosis prompt** – Classifies failure causes into predefined buckets and proposes the smallest next change.  
3. **Revision prompt** – Instructs the agent to revise using evaluator feedback, avoiding the same reasoning path if it already failed.  
4. **Memory‑summary prompt** – Summarizes attempts into short, reusable memory blocks: what failed, what worked, what to avoid, what to try next.  

These prompts operationalize the loop: each one plays a specific role in the attempt → evaluate → adapt → remember cycle.  

***

## Code‑level loop example

The Python‑style example in the article is intentionally minimal but captures the essential shape:

- Loop over attempts with `MAX_ATTEMPTS`.  
- On each attempt, build context from the task plus prior memory, then `plan` and `act`.  
- Run an `evaluator.check` to decide pass/fail and possibly whether human escalation is needed.  
- On failure, summarize what went wrong and append that to memory along with suggested fixes.  
- If escalation is required, return with status `"escalated"`; otherwise keep looping until success or `MAX_ATTEMPTS` is reached.  

The point is not the exact code; it’s the structure: **attempt → evaluate → store signal → adapt → retry or escalate**.  

***

## Where loop engineering matters most

The article calls out domains where single‑shot generation is fragile and loop quality dominates:

- **Coding agents** – natural fit: run tests, inspect failures, refine code, and stop only when verification passes.  
- **Support agents** – retrieve policies, draft responses, check grounding, escalate high‑risk or ambiguous cases.  
- **Research agents** – gather multiple sources, identify gaps, search again, synthesize only once evidence is sufficient.  
- **Document extraction agents** – parse and validate structured schemas, retry ambiguous fields, escalate low‑confidence cases.  
- **Operational workflow agents** – call APIs, check outcomes, recover from partial failure, route exceptions to humans.  

In all these, *loop design* is more important than getting the perfect one‑shot answer.  

***

## Common mistakes in loop engineering

The article warns about several failure patterns:

- Treating retries as improvement (more attempts without changed strategy).  
- Storing too much memory (context pollution and doom loops).  
- Skipping evaluation (no way to measure quality or progress).  
- No stop condition (unbounded cost and unpredictable behavior).  
- Ignoring human feedback (not encoding repeated corrections into the loop).  
- Using one giant loop for every task (no domain‑specific evaluators or strategies).  

Most of these come from treating the agent as a monolith instead of decomposing its loop by domain and task.  

***

## Recommended build sequence

The proposed build order is very practical:

1. **Define success clearly** – What does a good result look like?  
2. **Define failure categories** – What are the common ways this task goes wrong?  
3. **Build the evaluator first** – How will the system know success vs. failure?  
4. **Design memory rules** – What should persist between attempts?  
5. **Define retry strategy** – How does attempt two differ from attempt one?  
6. **Add escalation logic** – When and how do humans step in?  
7. **Log everything important** – Action traces, evaluations, and decisions for debugging and improvement.  
8. **Optimize only after the loop works** – Do not chase fancy patterns before the core cycle is stable.  

This keeps teams focused on *behavior* and observability rather than flashy autonomy.  

***

## Reality‑check checklist for “self‑improving” agents

The article ends with a litmus test—an agent only deserves the “self‑improving” label if:

- It knows what success looks like.  
- It evaluates each attempt against that definition.  
- It stores useful lessons from failures.  
- It changes strategy based on feedback.  
- It avoids blindly repeating failed paths.  
- It knows when to stop.  
- It knows when to escalate.  
- You can measure whether quality actually improves over time.  

If most of these are missing, the system is just iterative, not truly self‑improving.  

***

## Claude‑ready distilled keypoints (pasteable brief)

Here’s a compact block you can drop into Claude as context or a design brief:

> I am building self‑improving AI agents using loop engineering.  
> A self‑improving agent is not one that retrains itself, but one whose *loop* improves: each attempt uses signals from previous attempts to make better decisions.  
> The core loop is: Goal → Plan → Act → Observe result → Evaluate quality → Store signal → Retry / revise / escalate / finalize.  
> Improvement is driven by five components:  
> 1) Clear success criteria (tests, schemas, correctness or quality thresholds, human approval).  
> 2) An evaluation layer (rule checks, tests, LLM‑as‑judge, business logic) that decides pass/fail.  
> 3) Feedback memory that stores compact lessons: what failed, what worked, what to avoid, what to try next.  
> 4) Strategy revision that changes behavior between attempts (different tools, narrower tasks, more retrieval, clarification questions, or escalation).  
> 5) Boundaries and stop conditions (retry limits, cost/latency caps, protection around destructive actions, escalation rules).  
> My agents should never just “keep going.” They must evaluate each attempt, classify failures, adapt strategy, and remember only the signals that help future runs.  
> Strong improvement signals come from deterministic checks (tests, schemas, status codes), workflow metrics (retries, timings, failure step), human feedback (corrections, approvals), and comparisons across variants.  
> Prompting supports this loop with specialized prompts for: evaluation‑aware action, failure diagnosis, revision using evaluator feedback, and memory summaries.  
> I consider an agent truly “self‑improving” only if it: knows success, evaluates attempts, stores useful lessons, changes strategy based on feedback, avoids repeating failed paths, knows when to stop, knows when to escalate, and can be measured getting better over time. 
This piece is really three things glued together: a concrete **Claude + Obsidian second brain build**, a catalog of **15 agent patterns**, and a mindset shift toward **loop‑engineered, self‑correcting agents** rather than big prompts. You can feed Claude a distilled version of each layer and then ask it to specialize for your projects.

***

## Big picture

The author’s thesis is that you get the most leverage when you (1) externalize your memory into an Obsidian vault that models your life and work, (2) choose the *right* agent pattern for each task instead of cramming everything into one mega‑prompt, and (3) wrap all agents in verification‑driven loops so they improve instead of just doing more steps.  

This aligns with current best practice: second‑brain setups use Obsidian as the durable store with Claude/other LLMs as workers on top, agentic systems are built from a small set of reusable patterns, and “loop engineering” is where most real engineering effort now lives. [github](https://github.com/AgriciDaniel/claude-obsidian)

***

## Second-brain architecture (Claude + Obsidian)

**Two tools, two jobs.** Obsidian is the storage layer: local markdown files, wikilinks, and a visible graph; Claude is the reasoning and automation layer on top that reads, writes, links, and answers across the vault. This mirrors how real projects use an Obsidian vault (or similar) as the owned knowledge base and an LLM as an interface and organizer. [youtube](https://www.youtube.com/watch?v=AogwEfWZVds&time_continue=1118)

**Wire Claude into the vault via MCP.** The Local REST API plugin exposes Obsidian as an HTTP server, and an MCP server (`mcp-obsidian`) bridges that into Claude Code so the model can list, read, and write notes. Community MCP servers and guides replicate this exact pattern: Obsidian plugin on localhost, MCP process that talks to it, and Claude tools that call the MCP. [youtube](https://www.youtube.com/watch?v=VeTnndXyJQI)

**Vault structure for long-term leverage.** The article pushes Karpathy’s “LLM Wiki” pattern:  
- `raw/` for immutable sources (articles, transcripts, screenshots) that Claude can read but never edits.  
- `wiki/` for generated summaries, concept pages, and cross‑references.  
- `CLAUDE.md` at vault root containing your identity, goals, preferences, and current projects so every session starts with your context pre‑loaded.  

Existing “Claude second brain” projects use very similar layouts and show this works at scale: drop sources in, let the model compile the wiki, and use lint steps to catch broken links or contradictions. [youtube](https://www.youtube.com/watch?v=C6b1bX1HNg8)

**Teach Claude your dialect.** Steph Ango’s `obsidian-skills` repo serves as a formal spec for Obsidian‑flavored markdown, Bases, Canvas, and CLI commands, giving Claude explicit instructions and examples so it generates *valid* Obsidian content instead of “almost right” markdown. [youtube](https://www.youtube.com/watch?v=YwisPa5vEfM)

**Automate live workflows.** The author extends beyond static notes by connecting live tools (e.g., Google Calendar) via MCP servers (e.g., `workspace-mcp --tools calendar`) and scheduled tasks. This allows daily routines like: ingest new files into `raw/`, update `wiki/`, flag stale notes, and write a morning change summary at 7 a.m. Similar automations appear in tutorials where Claude runs morning briefings and end‑of‑day debriefs across calendar, tasks, and wiki. [youtube](https://www.youtube.com/watch?v=VeTnndXyJQI)

**Security stance.** The piece is very explicit that access should be controlled by *keys*, not prompt instructions. Read‑only scoped keys and MCP servers that expose only safe operations are required; “please don’t delete X” in a prompt is treated as cosmetic and insufficient. [github](https://github.com/Trao95/Obsidian-MCP-Server)

**If you do not want to wire from scratch.** The three recommended repos (claude‑obsidian, obsidian‑second‑brain, second‑brain‑starter) are packaged versions of this pattern: they ship Claude Code skills, slash commands, and scaffolders for common knowledge‑management methods (PARA, Zettelkasten, etc.). This mirrors a broader ecosystem of “second brain with Claude + Obsidian” templates and courses. [github](https://github.com/AgriciDaniel/claude-obsidian)

***

## Agent patterns: 15 ways to structure behavior

The agent-pattern section is a **design vocabulary** for how to architect agents once basic prompting plus tools stops scaling. It emphasizes that you should only choose an agent pattern if a single model call is not enough (multiple tools, planning, uncertainty, or validation required). [vdf](https://vdf.ai/blog/agentic-design-patterns-practical-guide/)

### Single-agent patterns

1. **Single Agent** – One model, one prompt, small tool set; good when the job is narrow and context fits in one head.  
2. **Loop** – Repeat a fixed sequence until an exit condition; ideal for multi‑pass tasks like data cleaning with a clear quality gate.  
3. **ReAct (Reason and Act)** – Alternating reasoning and tool‑use where the next step depends on tool results; fits exploratory investigations.  

These echo research and industry patterns like ReAct, reflection, and simple retry loops as core building blocks. [aitoolsclub](https://aitoolsclub.com/15-agentic-ai-design-patterns-you-should-know-research-backed-and-emerging-frameworks-2026/)

### Multi-agent orchestration patterns

4. **Multi-Agent Sequential** – Fixed linear pipeline: each specialist agent feeds the next (e.g., extract → analyze risk → summarize contract).  
5. **Multi-Agent Parallel** – Independent agents work concurrently, then their results are merged (e.g., multi‑source incident investigation).  
6. **Coordinator** – Routing agent sends requests to the right specialist based on type (billing vs. technical vs. fraud).  
7. **Hierarchical Task Decomposition** – Top‑level “manager” agent breaks a broad goal into subgoals and delegates to workers.  
8. **Swarm** – Multiple specialist agents debate or collaborate in a shared conversation, with a facilitator synthesizing the outcome.  

These map closely to orchestrator–subagent, supervisor, and multi‑agent coordination patterns in current architecture guides. [theaiuniversity](https://theaiuniversity.com/docs/building-agents/architecture-patterns)

### Feedback & quality patterns

9. **Review and Critique** – A separate judge agent critiques another agent’s output.  
10. **Iterative Refinement** – Generator refines outputs based on a quality score until crossing a threshold.  
11. **Reflexion** – Agent evaluates its own failures and carries lessons forward between attempts.  
12. **Human-in-the-Loop** – Human approval for risky decisions or edge cases.  
13. **Plan-and-Execute** – Dedicated planner produces an explicit plan that an executor follows, often with human review of the plan.  
14. **Custom Logic** – Code handles deterministic rules and permissions; the model handles judgment, drafting, and exceptions.  
15. **Event-Driven Agent** – Agent reacts to external events instead of incoming user prompts (fraud detection, ops alerts, etc.).  

These reflect a broader consensus: you get more reliability by combining LLMs with deterministic validation, human checkpoints, and explicit planning rather than trying to put everything in a single system prompt. [docs.aws.amazon](https://docs.aws.amazon.com/prescriptive-guidance/latest/agentic-ai-patterns/workflow-for-evaluators-and-reflect-refine-loops.html)

**Pattern selection rule.** The author’s key heuristic: **match the pattern to the dominant uncertainty** in the task—tool choice, routing, quality, path, self‑correction, risk, problem structure, or timing—rather than chasing the fanciest architecture.  

***

## Loop engineering mindset

The loop‑engineering section reframes the job of an AI engineer: you are not just writing prompts; you are designing loops and verifiers around a commodity model. Materials like loop‑engineering docs and agent‑loop explainers make the same point: every serious framework implements a similar “while loop,” and the differentiation is in stop conditions, verification, and context management. [ithub.global.ssl.fastly](https://ithub.global.ssl.fastly.net/cocodedk/loop-engineering/blob/main/docs/04-loop-anatomy.md)

**Canonical loop.** At its core, an agent loop is:

> `while` model has more tool calls:  
>  call tools → update context → call model again  

Everyone agrees the loop itself is trivial; what matters is **what happens around it**—budget caps, evaluation, error handling, and context hygiene.  

**Hard problems the loop must solve:**

- **Knowing when to stop.** Distinguish “turn is over” from “task is done;” enforce max iterations, token/time budgets, no‑progress detection, and real completion checks (tests, validations, or other hard conditions).  
- **Preventing context rot.** Summarize long histories (compaction), offload big artifacts (files, logs, notebooks), and isolate messy subtasks into sub‑agents to avoid doom‑loops where noisy context makes each step worse. [blakecrosley](https://blakecrosley.com/ko/blog/loops-win-where-verification-is-cheap)
- **Designing usable tools.** Small, non‑overlapping tool sets; idempotent writes; and error messages optimized for LLMs (“what to do next”), not humans.  
- **Putting a critic in the loop.** Split worker vs. checker; use tests, type checks, or a judge model so “done” is proven rather than just asserted by the agent.  

This matches evaluator–optimizer loops and reflection patterns where cheap verification gates determine what can be safely automated. [hopx](https://hopx.ai/blog/ai-agents/evaluator-optimizer-loop/)

**Shift in engineer’s job.** Instead of “what prompt should I send?”, the key questions become: What does success look like in a way the agent can check? What loop do we run around the model? What verifier and guardrails prove completion? Cherny’s framing (“I don’t prompt; I write loops”) captures this shift. [ithub.global.ssl.fastly](https://ithub.global.ssl.fastly.net/cocodedk/loop-engineering/blob/main/docs/04-loop-anatomy.md)

***

## Design rules for production agents

The closing “10 rules” are basically operational guardrails:

- Start with the **simplest pattern** that works; do not jump to multi‑agent swarms until single‑agent designs are exhausted.  
- Treat **tool descriptions as contracts**: the only source of truth the model has about what a tool does.  
- Cap **iterations, tool calls, and spend** so agents cannot accidentally burn money or time.  
- Log full **action traces** (tool calls, arguments, outputs, final decisions) for debugging and evaluation.  
- Put irreversible actions behind **deterministic checks or human approvals**; never let the model alone control money movement or production changes.  
- Evaluate on **real failure cases** and edge conditions, not just happy paths.  
- Split prompts by **responsibility**; if your system prompt is full of “but don’t do X when Y,” you probably need separate agents.  
- Treat multi‑agent setups as **distributed systems**: expect partial failures, latency, timeouts, retries, and the need for observability.  
- Use **model review to improve quality**, but never as a substitute for hard validation checks.  
- Prefer **simpler patterns** to save complexity budget for better tools, prompts, and evaluation.  

These rules line up with guidance from modern production agentic frameworks and design docs. [vdf](https://vdf.ai/blog/agentic-design-patterns-practical-guide/)

***

## Claude-ready distilled context (paste this into a vault note)

You can drop this block into `CLAUDE.md` or a “Agent Patterns + Loops” wiki page:

> I am building systems where:  
> – **Obsidian** is the durable storage layer (markdown files, wikilinks, graph).  
> – **Claude** is the reasoning and automation layer that reads/writes the vault, links notes, and runs scheduled workflows across tools via MCP.  
> – The vault follows an **LLM Wiki** structure: `raw/` for immutable sources, `wiki/` for generated pages, and `CLAUDE.md` for my identity, goals, preferences, and active projects.  
> – Claude should use Obsidian‑specific skills (wikilinks, Canvas, Bases, CLI) so outputs are valid Obsidian documents.  
> – For automation, Claude may run daily jobs (e.g., ingest new sources, update wiki, flag stale notes, summarize changes) and connect to live tools like calendar via MCP with scoped keys and read‑only access where possible.  
> – All agents are built from explicit **patterns** (Single Agent, Loop, Multi‑Agent Sequential/Parallel, Coordinator, Hierarchical, Swarm, ReAct, Human‑in‑the‑Loop, Plan‑and‑Execute, Reflexion, Custom Logic, Event‑Driven). The pattern is chosen based on the main uncertainty (tool choice, routing, quality, execution path, self‑correction, risk, problem structure, or timing), not hype.  
> – Agents must run inside explicit **loops**: Goal → Plan → Act → Observe → Evaluate → Store signal → Retry / revise / escalate / finalize.  
> – Every loop has:  
>   1) Clear success criteria the system can check (tests, schemas, grounding, business rules, human approval).  
>   2) An evaluation layer that classifies failures and decides pass/fail.  
>   3) Feedback memory that stores compact lessons (what failed, what worked, what to avoid, what to try next).  
>   4) Strategy revision so each attempt can change tools, prompts, retrieval, or escalation plan.  
>   5) Hard boundaries: max attempts, budget and time limits, guardrails around destructive actions, and well‑defined escalation to humans.  
> – Tools must be few, focused, and described like contracts; write operations should be safe to repeat; error messages should tell the agent what to do next.  
> – I only call an agent “self‑improving” if it: knows success, evaluates each attempt, stores useful lessons, changes strategy based on feedback, avoids repeating failed paths, knows when to stop, knows when to escalate, and shows measurable improvement over time.  

If you want, I can next turn this into a structured `CLAUDE.md` for *your* profile plus a minimal Obsidian folder blueprint you can drop straight into a repo.

How to Build a Content Machine with Claude That Runs Without You
learn how to set up Claude as a full content operation. configure your agent roles, build the production pipeline, and turn a Claude Project into a machine that researches, writes, reviews, and publishes, while you do other work.
most people use Claude to write one thing at a time. they open a chat, type a prompt, get output, close the tab. the next day they do it again from scratch. that is not a content operation. that is manual labor with an AI keyboard.
a content machine is different. it is a set of agents with defined roles, a shared memory file they all read from, and a pipeline that moves a topic from idea to published output with minimal input from you. this article shows you exactly how to build it.
Understand the Structure Before You Build
-------------------------------------------
a content machine has four roles: researcher, writer, editor, and publisher. each role is a separate Claude Project with its own system prompt and its own job. no single agent does everything.
the researcher finds angles, pulls references, and writes a brief. the writer turns that brief into a draft. the editor reviews against your brand voice and flags weak sections. the publisher formats the output and prepares it for wherever it goes.
a shared file called `PIPELINE.md` sits at the center. every agent reads it at the start of a session and writes its output back to it. this is the memory that connects four separate agents into one working system.
Set Up the Researcher Agent
------------------------------
open claude.ai and create a new Project. name it "Content Researcher." paste this as the system prompt:
---
you are a content researcher. your job is to take a topic and return a structured brief.
read PIPELINE.md first. find the topic listed under "NEXT UP." research it and return:
- one sharp angle (not the obvious take)
- three supporting points with real specifics
- two sources worth referencing
- one question the audience is actually asking
write your brief under "BRIEF" in PIPELINE.md. mark the topic as "BRIEFED."
---
give it a real topic on the first run: "write a brief on how solopreneurs use Claude Projects to replace hiring." review what it returns. correct anything that missed your standard. that correction becomes the new benchmark for every run after.
Set Up the Writer Agent
--------------------------
create a second Project. name it "Content Writer." paste this system prompt:
---
you are a content writer. your job is to turn a brief into a finished first draft.
read PIPELINE.md first. find the brief listed under "BRIEF." write the article using this format:
- hook: one sentence that names the outcome upfront
- body: short paragraphs, max 4 lines each, plain language, no filler
- no conclusion that summarizes what was already said
use the voice in VOICE.md. save your draft under "DRAFT" in PIPELINE.md. mark the status as "DRAFTED."
---
VOICE.md is a separate file you write once. put five examples of your best existing content in it. tell the agent: "this is the voice, rhythm, and format every article must match." it reads this file every session and writes to match it.
Set Up the Editor Agent
--------------------------
create a third Project. name it "Content Editor." paste this system prompt:
---
you are a content editor. your job is to review a draft and return specific fixes, not rewrites.
read PIPELINE.md first. find the draft under "DRAFT." check it against VOICE.md. return:
- any paragraph that exceeds 4 lines: flag it, suggest how to cut
- any sentence that has filler or restates something already said: quote it, delete it
- any section where the reader stops learning something new: identify the line
- a verdict: APPROVE or REVISE
do not rewrite the draft. return only precise surgical feedback. save your notes under "EDITS" in PIPELINE.md. mark status as "EDITED."
---
the editor's job is not to like the draft. its job is to catch what the writer missed. the separation between writer and editor is what stops the machine from approving its own mediocre work.
Set Up the Publisher Agent
-----------------------------
create a fourth Project. name it "Content Publisher." paste this system prompt:
---
you are a content publisher. your job is to take an approved draft and prepare it for publishing.
read PIPELINE.md first. confirm the status reads "APPROVED." format the article for [your platform: Substack / Ghost / Medium / LinkedIn]. return:
- the formatted article ready to paste
- a title option and an alternate title
- a two-sentence description for the preview card
- three hashtags if the platform uses them
save the final output under "READY" in PIPELINE.md. mark status as "PUBLISHED."
---
swap the platform name for whatever you actually publish on. if you publish on multiple platforms, create one publisher agent per platform. each formats the same approved draft differently.
Build the PIPELINE.md File
-----------------------------
create a plain markdown file called PIPELINE.md. this is the connective tissue of the whole system. every agent reads and writes to it. structure it exactly like this:
---
# Content Pipeline
## NEXT UP
- topic goes here
## BRIEF
[researcher writes here]
## DRAFT
[writer writes here]
## EDITS
[editor writes here]
## READY
[publisher writes here]
## STATUS
current stage: [BRIEFED / DRAFTED / EDITED / APPROVED / PUBLISHED]
---
upload this file to every Claude Project as a piece of Project knowledge. when an agent finishes its job, it writes back to the relevant section and updates the STATUS line. the next agent picks up from there.
Run the Machine for the First Time
-------------------------------------
drop a topic into PIPELINE.md under "NEXT UP." open the Researcher Project, type: "read PIPELINE.md and complete your job." review the brief it returns. if it is solid, update the STATUS to BRIEFED.
open the Writer Project. type: "read PIPELINE.md and complete your job." it reads the brief and returns a draft. review it. if it matches your voice, update the STATUS to DRAFTED.
open the Editor Project. type: "read PIPELINE.md and complete your job." it reviews the draft and returns specific edits. apply the ones that are right. when you are satisfied, update the STATUS to APPROVED.
open the Publisher Project. type: "read PIPELINE.md and complete your job." it returns the formatted, ready-to-publish article. paste it directly into your platform.
the first run is manual and slow. that is correct. you are not automating yet. you are verifying that each agent does its job well before you step back.
Add Scheduling Once Each Agent Is Reliable
--------------------------------------------
Claude Code has a built-in scheduling system. once you are confident in each agent's output, go to the Claude desktop app, open Claude Code, and run `/schedule` to create a routine for each stage.
a practical starting schedule: researcher runs at 7am and drops a brief into PIPELINE.md. you review and approve by 9am. writer runs at 10am and returns a draft by noon. you review and approve. editor runs at 1pm. you apply the edits. publisher runs at 3pm. you paste and publish.
your input is four approval moments, not four creation sessions. the agents handle the production. you handle the judgment.
Keep the Machine Improving
-----------------------------
every time an agent produces something that misses the mark, update its system prompt with the specific correction. do not rely on memory. the prompt is the agent's operating standard. corrections that stay only in your head die the next time you open the Project.
once a week, read the last five published articles and check them against your VOICE.md file. if the voice has drifted or improved, update VOICE.md to reflect the current standard. all four agents read from it. one update tightens the whole machine.
the machine does not improve on its own. you improve it. but the inputs get smaller and the outputs get better each week because the corrections compound across every future run.
What Your Job Becomes
-----------------------
you are not the writer anymore. you are the editor-in-chief. your job is to set the standard, approve or reject what the agents produce, and update the system when it drifts.
this shift in role is where the actual leverage lives. a writer can produce one or two pieces a week. an editor-in-chief running four agents can review and approve five to ten, with each piece arriving already researched, drafted, and formatted.
the machine runs without you in the production sense. it does not run without your judgment at the approval gates. keep those gates. that is the part that keeps the output worth publishing.

I DON'T KNOW WHAT YOU'RE DOING IF YOU'RE NOT USING THIS.

 Andrej Karpathy reads an article and never loses it. He coined "vibe coding," and he still says the same thing about memory: you can outsource your thinking, but you can't outsource your understanding.

Most people pile articles into a vault and never reopen them. Karpathy reopens his.

He pulls every article he reads into a wiki inside Obsidian. Claude rebuilds and reorders it, then hands it back from a new angle. He interrogates it until the idea sticks.

The full Claude and Obsidian setup is in the article below. Vault structure, the wiki prompt, the question loop.

Open it before your vault fills with notes you forgot you saved.

https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md

https://github.com/NousResearch/hermes-agent

https://github.com/techjarves/hermes-usb-portable

https://github.com/SebReyWriter/TheBLUEForum/discussions/1

https://github.com/anthropics/claude-code
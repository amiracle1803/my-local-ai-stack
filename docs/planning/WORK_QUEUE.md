# Builder Work Queue

Fable = manager only. Builders: **Haiku** = mechanical (installs, moves,
inventory), **Sonnet** = code, **Opus** = escalation only. Keep work orders
lean (embed only needed excerpts). Fable verifies gates + commits; builders
never run git.

## ACCEPTED
- M0 pipeline skeleton (a0755e1) · WF-1 workflows (4e7b3e8) · KREA-1 sourcing
  (bef70d1) · WF-2 placeholder/sidebar cleanup (215b9aa) · M1 Stage 0B
  (16d8167 — real 610-word script generated by qwen3:8b, 53 tests green)

## IN FLIGHT (resumed after session-limit death)
- **COMFY-UPDATE** (Sonnet) — ComfyUI 0.24.0 -> **0.27.1 DONE**; all 10
  templates revalidated. Remaining: krea2 proof-render evidence + fill
  krea2-lab-report.md §4-§6 + node-additions-plan version line.
- **STUDIO-API** (Sonnet) — kernel/pipeline_api.py written + mounted in
  app.py; remaining: Studio page project selector/new-project form/status
  dots, tests, verification.

## QUEUED
1. **STORAGE-1 (Haiku)** — ComfyUI is bloated; move bulk to E:.
   Mechanism: `C:\AI\ComfyUI\extra_model_paths.yaml` (ComfyUI's native way
   to read models from other drives). Move to `E:\AI\ComfyUI-models\`:
   banned checkpoints (NoobAI-XL 6.62 GB, wai-illustrious 6.46 GB) and the
   unused encoders (qwen3vl_8b_fp8 9.86, qwen3vl_8b_nvfp4 5.87,
   umt5-xxl-enc-bf16 10.58 — krea2 uses the 4b, Wan uses umt5 fp8). Keep on
   C: only what active templates load. Copy-verify-delete; E: may drop
   offline (Test-Path first). Also: prune `C:\AI\ComfyUI\.git` bloat only if
   safe, clear `temp/`, and report the before/after `du`. Also install the
   pip Manager (`pip install -U --pre comfyui-manager` into ComfyUI's venv,
   NOT the stack venv) and add `--enable-manager` to the launch args in
   `olympus/kernel/services.py` — that is what 0.27's missing-node dialog
   asks for. Note: `ResizeImageMaskNode` is NOT referenced by any of our
   templates (verified) — no template fix needed.
2. **INSTALL-1-closeout (Haiku)** — kohya `sdxl_train_network.py --help`
   sanity, anime-face-detector decision, fill node-additions-plan.md §B.
3. **M2 (Sonnet)** — Stage 1 world bible (original spec Steps 1-5 + v2
   deltas: era inference, relationship web, provenance, lexicon, wardrobe
   events). Biggest stage; split into M2a (scan+profiles) / M2b (world+
   contradictions+expansion) if the order gets long.

## KNOWN ISSUE (blocks M4 LoRA training, not urgent)
- kohya at `E:\AI\kohya_ss`: venv is sound (torch 2.6.0+cu124, CUDA True) but
  the train scripts fail argument parsing on `--help`. Diagnose before M4
  (item 2 of node-additions-plan §B depends on it). Likely a sd-scripts
  version/entrypoint change — check the repo's current CLI entry points.

## STANDING FACTS FOR BUILDERS
- ComfyUI: `C:\AI\ComfyUI` (git checkout, now 0.27.1), venv pins
  torch==2.6.0+cu124 / pydantic 2.12.3 must survive. `E:\AI\ComfyUI` = weekly
  mirror (task SyncComfyUI). There is NO portable `update/` folder.
- MODEL BAN LIST: z-anime-distill, wai-illustrious-v110, NoobAI-XL-v1.1,
  FLUX.1-Krea-dev. krea2 = krea/Krea-2-Turbo.
- git: `C:\Program Files\Git\cmd\git.exe` (not on PowerShell PATH).
- Use stack-venv python + urllib for `/object_info` (PowerShell chokes).

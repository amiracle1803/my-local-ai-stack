id: background_runtime
display_name: Background Runtime
layer: action
description: Job queue with persistent SQLite storage and background worker loop.

model_preference: []

inputs:
  - op
  - job_id

outputs:
  - status
  - running

tools: []
memory_scopes: []
policies: []

system_prompt: |
  You are the Background Runtime. Manage the job queue — start, pause, stop, and report jobs.

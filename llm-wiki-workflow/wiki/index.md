# Wiki Index

_Auto-generated by `wiki index` on 2026-06-23. Do not edit by hand._

## Concepts
- `concepts/agent-atlas-architecture.md` – Full system design of Agent Atlas — how tasks flow from web UI through 18 agents to Obsidian memory.
- `concepts/background-job-queue.md` – Agent Atlas's async task queue using SQLite as the backing store with atomic BEGIN IMMEDIATE claims to prevent double-execution by concurrent workers.
- `concepts/collaboration-bus.md` – The async message-passing backbone of Agent Atlas — every agent-to-agent call is persisted, traced, and broadcast over WebSocket.
- `concepts/idea-file.md` – A portable natural-language document describing a pattern (not an implementation), designed to be instantiated by an LLM agent for each user's specific environment.
- `concepts/layered-agent-architecture.md` – Agent Atlas organizes 18 agents into 4 functional layers — Control, Knowledge, Action, and Platform — each with distinct responsibilities and interaction patterns.
- `concepts/llm-wiki-workflow.md` – Three-layer knowledge system — immutable raw sources, a generated markdown wiki, and a schema that governs ingest/query/lint. Knowledge is compiled once on ingest, not re-derived on every query.
- `concepts/memex.md` – Vannevar Bush's 1945 hypothetical personal knowledge device with associative trails — the conceptual predecessor to the LLM wiki, hypertext, and the World Wide Web.
- `concepts/model-router.md` – Agent Atlas auto-discovers available LLMs (local-first) and routes each agent to the best available model based on declared preferences and live probe results.
- `concepts/policy-engine.md` – The Guardian agent's rule evaluation system — YAML-defined policies (block/warn/allow) evaluated top-to-bottom before any destructive tool call.
- `concepts/retrieval-augmented-generation.md` – Pattern where a model retrieves relevant documents at query time and conditions its answer on them.

## Entities
- `entities/agent-atlas-system.md` – Local-first multi-agent AI system with 18 specialized agents, FastAPI backend, React web UI, and Obsidian memory integration.
- `entities/andrej-karpathy.md` – AI researcher, co-founder of OpenAI, former Tesla AI lead, coined "vibe coding," and originator of the LLM Wiki pattern via a viral April 2026 tweet and GitHub gist.
- `entities/atlas-action-hub.md` – Routes action requests to Code Agent or Automation Agent, with mandatory Guardian policy check before any destructive operation.
- `entities/atlas-agent-factory.md` – Dynamically creates new agents at runtime via the /factory web UI or POST /api/agents/factory — writes YAML config and registers with the collaboration bus without restarting.
- `entities/atlas-automation-agent.md` – Creates and manages scheduled and recurring tasks. Writes automation scripts and enqueues them as background jobs.
- `entities/atlas-background-runtime.md` – Multi-worker async job executor — polls the SQLite job queue using atomic BEGIN IMMEDIATE claims to dispatch queued tasks to the correct agent without double-execution.
- `entities/atlas-code-agent.md` – Generates, edits, and executes code using LLM + repo-map context. All file writes and shell executions are policy-gated by Guardian.
- `entities/atlas-creative-studio.md` – Generates structured documents — reports, proposals, marketing copy, technical write-ups — using LLM with user-provided template and tone preferences.
- `entities/atlas-deployment-agent.md` – Manages deployment operations — status checks, log retrieval, and rollback coordination for services managed by Agent Atlas.
- `entities/atlas-evaluator.md` – Scores agent outputs on a 0-10 scale and requests retries when quality falls below threshold. No direct side effects — review only.
- `entities/atlas-guardian.md` – The policy enforcement agent — evaluates every destructive tool call against config/policies.yml rules and returns block/warn/allow decisions before execution.
- `entities/atlas-hermes-bridge.md` – Auto-detects and registers locally running LLM servers (Ollama, LM Studio) so the Model Router always knows what models are currently available without manual configuration.
- `entities/atlas-knowledge-hub.md` – Fan-out knowledge retrieval coordinator — runs ChromaDB vector search and Obsidian vault search in parallel and returns a merged evidence pack.
- `entities/atlas-local-model-trainer.md` – Prepares fine-tuning datasets from Agent Atlas's own run history and Obsidian notes, enabling local model improvement over time.
- `entities/atlas-memory-agent.md` – Manages three scopes of memory (profile, project, episodic) in SQLite and mirrors all writes to the Obsidian vault automatically.
- `entities/atlas-observability-agent.md` – Collects and analyzes agent performance metrics — call counts, latency, error rates — exposed via /api/metrics and the /logs web UI page.
- `entities/atlas-obsidian-brain.md` – Semantic search, summarize, and append to the Obsidian vault — the bridge between Agent Atlas and your personal knowledge base, including this LLM Wiki.
- `entities/atlas-orchestrator.md` – Control layer entrypoint — classifies user goals as trivial or complex, coordinates the full execution plan, and aggregates results from all delegated agents.
- `entities/atlas-planner.md` – Breaks complex user goals into a JSON subtask DAG with agent assignments and dependency ordering.
- `entities/atlas-retrieval-agent.md` – Queries the ChromaDB vector store for semantically relevant documents. Handles non-Obsidian knowledge (ingested docs, code snippets, raw data).
- `entities/local-llm-runtimes.md` – Tools that run language models on local hardware (e.g. Ollama, llama.cpp, LM Studio).
- `entities/obsidian.md` – Local-first markdown knowledge management app with bidirectional links, graph view, and a plugin ecosystem — the recommended viewer/IDE for the LLM wiki layer.
- `entities/qmd-search.md` – Local markdown search engine by Tobi Lutke (Shopify CEO) combining BM25 keyword search, vector semantic search, and LLM re-ranking — all on-device via node-llama-cpp.

## Source Summaries
- `sources/karpathy-llm-wiki-complete-guide.md` – Deep-dive covering Karpathy's three-layer LLM wiki pattern — raw sources, generated wiki, schema — with tool stack, operations, implementation stages, and Memex connection.
- `sources/summary-build-this-workflow.md` – Notes describing the three-layer wiki architecture and the ingest/query/lint operations.

## Comparisons
- `comparisons/wiki-vs-rag.md` – Full tradeoff comparison between a curated LLM-maintained markdown wiki and a vector-index RAG pipeline across seven dimensions.

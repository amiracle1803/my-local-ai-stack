# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Keep .vscode (shared tasks) but ignore local-only state
.vscode/*.log

# Watcher state (local per-machine)
.watcher-state

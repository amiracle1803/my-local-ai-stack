# Build This Workflow — source notes

Placeholder for an immutable source document. In real use, you would drop the
original article / paper / readme here unchanged. This file exists so the
example source-summary page has a real `sources:` target.

Key idea captured: a three-layer wiki (raw sources -> generated wiki -> schema)
maintained through ingest, query, and lint operations.

# raw/ — immutable sources

Everything in `raw/` is **source-of-truth** and must never be modified by the
agent. Drop original articles, papers, repo readmes, datasets, and images here
unchanged. The wiki under `wiki/` is generated *from* these.

# OPTIONAL upgrades. You do NOT need these to run any of the three projects.
# They are heavier (bigger downloads, occasionally fiddly on Windows), so
# install them only when you actually want the feature.
#
# Install with:  .venv\Scripts\python -m pip install -r requirements-optional.txt

pymupdf4llm>=0.0.17     # PDF -> Markdown. Installed. Used by shared/lib/pdf.py.
crawl4ai>=0.9           # JS-capable web crawler. Installed -- also run once:
                        #   .venv\Scripts\python -m playwright install chromium
                        # Used by shared/lib/webfetch.py's prefer_crawl4ai path;
                        # opt in per-run via config.json's "prefer_crawl4ai" key
                        # (defaults to false -- trafilatura stays the default).
                        # Note: pins lxml~=5.3 but this repo runs lxml>=6.1.1
                        # for trafilatura; verified compatible at runtime.
# marker-pdf>=1.0       # heavy ML PDF->Markdown for very complex layouts (pulls torch)
# langfuse>=2.50        # LLM observability/tracing if you run the Langfuse container

# --- dev / test tooling ---
pytest>=8.0             # test runner for the anime pipeline v2 suite
                        # (olympus/engines/pipeline/tests). Installed 2026-07-09.
opencv-python-headless==4.10.0.84  # anime-face bbox detector (design 3C.4) --
                        # pipeline/facedetect.py, backed by the lbpcascade
                        # cascade in olympus/engines/pipeline/assets/. Pinned:
                        # opencv-python-headless 5.x (as of 2026-07) ships
                        # without cv2.CascadeClassifier (objdetect not built
                        # in); 4.10.0.84 is the last known-good headless build
                        # with the Haar/LBP cascade API. Installed 2026-07-10.

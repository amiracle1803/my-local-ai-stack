# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# MCP (Model Context Protocol) server — expose agents to Claude Desktop / Cursor / VS Code
mcp>=1.0.0

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# HTTP client (local LLM + Groq calls)
httpx>=0.27.0

# Pydantic
pydantic>=2.7.0

# Vector store
chromadb>=0.5.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
